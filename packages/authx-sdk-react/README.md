# @authx-rs/sdk-react

React bindings for `@authx-rs/sdk-web`.

Current scope:

- provider for `AuthxTokenManager`
- hooks for snapshot, access token, and authenticated fetch
- thin integration layer only; storage and refresh logic stay in `@authx-rs/sdk-web`
