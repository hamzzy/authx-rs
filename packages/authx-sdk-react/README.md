# @authx/sdk-react

React bindings for `@authx/sdk-web`.

Current scope:

- provider for `AuthxTokenManager`
- hooks for snapshot, access token, and authenticated fetch
- thin integration layer only; storage and refresh logic stay in `@authx/sdk-web`
